# Docker

Docker-related project files live here.

Current local entrypoint:

```text
../docker-compose.yml
```

Current backend image definition:

```text
../backend/Dockerfile
```

Detailed launch instructions:

```text
../docs/docker_setup_ua.md
```

This directory is reserved for future Docker assets such as reverse proxy configuration, local overrides, database init scripts, and production deployment templates.
