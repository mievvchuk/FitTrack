# Database Migrations

PostgreSQL migrations are managed by Alembic in:

```text
backend/alembic/versions/
```

This directory is kept as the database-level index for coursework repository structure. Use it for SQL-only migration notes or exported migration snapshots if required by the university.
