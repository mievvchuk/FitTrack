# Mobile Tests

Place Flutter tests here.

Suggested groups:

- `widget/` - screens and reusable widgets;
- `integration/` - login, workout builder, Premium checkout, localization;
- `golden/` - stable UI snapshots for important screens.
