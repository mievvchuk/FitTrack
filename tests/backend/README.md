# Backend Tests

Place FastAPI and Python tests here.

Suggested groups:

- `unit/` - services, calculations, validators, repositories;
- `integration/` - API endpoints, PostgreSQL, Stripe test flow;
- `security/` - JWT, refresh tokens, permissions, rate limiting.
